import { validateForm } from './functions.js';

document.addEventListener('DOMContentLoaded', () => {

    //Cuando cargue el documento HTML, validar el formulario de registro.
    validateForm();
});