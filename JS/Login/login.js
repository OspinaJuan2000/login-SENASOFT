import { validateForm } from './functions.js';

document.addEventListener('DOMContentLoaded', () => {

    //Cuando cargue el documento HTML, validar el formulario de inicio de sesión.
    validateForm();
});